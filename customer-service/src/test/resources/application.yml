# Config para tests: no arrancar config-server ni eureka.
spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
  config:
    import: "optional:configserver:"
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
